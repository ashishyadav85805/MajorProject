@model IEnumerable<NGOManagementSystem.Models.DonorInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DonorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DonorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DonorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DonorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DonorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<div class="btn btn-dark">
    <a asp-area="Hub" asp-controller="DonorDetails" asp-action="Create" style="color:white">Next</a>
</div>