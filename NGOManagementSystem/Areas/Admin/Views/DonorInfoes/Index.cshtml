@model IEnumerable<NGOManagementSystem.Models.DonorInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
@*    <a class="btn btn-dark" asp-action="Create">Create New</a>*@
</p>
<br/>
<table class="table">
    <thead>
        <tr style="background-color:lightblue">
            
            <th>
                @Html.DisplayNameFor(model => model.DonorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
            <tr style="background-color:lightgray">
                
            <td>
                @Html.DisplayFor(modelItem => item.DonorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
@*                    <a class="btn btn-dark" asp-action="Edit" asp-route-id="@item.DonorId">Edit</a> |*@
                    <a class="btn btn-dark" asp-action="Details" asp-route-id="@item.DonorId">Details</a> |
                <a class="btn btn-dark" asp-action="Delete" asp-route-id="@item.DonorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
<div class="btn btn-dark">
    <a asp-area="Admin" asp-controller="DonorDetails" asp-action="Create" style="color:white">Next</a>
</div>